<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.LLMDroid" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize light theme attributes here -->
        <!-- Example: <item name="colorPrimary">@color/purple_500</item> -->
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/purple_500</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.LLMDroid</item>
    </style>

</resources>